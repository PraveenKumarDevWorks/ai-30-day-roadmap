# Postgres started by docker-compose.yml (user/password/db match that file)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/day4_search"

# Ollama — used here only for embeddings, not chat/generate
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_EMBED_MODEL=nomic-embed-text
